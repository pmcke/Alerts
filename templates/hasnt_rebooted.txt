Station {station} has not rebooted recently.

Last boot (UTC): {lastboot}
Hours since boot: {hours}

Suggested fix:
1) Check power/network
2) SSH into the Pi and reboot:
   sudo reboot
3) If it returns, check camera service logs.

Unsubscribe:
{unsubscribe_link}
