Camera alert for station {station}

No messages have been received since:
{time}

This indicates the camera may be offline.

If you no longer wish to receive these alerts, unsubscribe here:
{sig = HMAC_SHA256(secret, f"{station}|{email}")
unsubscribe_link = f"{base}/unsubscribe?station={station}&email={email}&sig={sig}"
}
